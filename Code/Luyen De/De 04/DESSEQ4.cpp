#include<bits/stdc++.h>
using namespace std;
long long n, a[1000009], dem = 1, gmax = 0, l = 0, r = 0;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    freopen("DESSEQ4.inp","r",stdin);
    freopen("DESSEQ4.out","w",stdout);
    cin>>n;
    for(int i = 1; i <= n; i++)
        cin>>a[i];
    for(int i = 1; i <= n; i++){
        for(int j = i+1; j <= n; j++){
            if(a[j] >= a[j-1])

        }
    }
}

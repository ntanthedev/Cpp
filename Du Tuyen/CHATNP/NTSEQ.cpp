#include<bits/stdc++.h>

using namespace std;

signed main() {
    freopen("NTSEQ.inp","r",stdin);
    freopen("NTSEQ.out","w",stdout);

    int n;
    cin >> n;
    vector<int> a(n);
    for(auto &i : a)
        cin >> i;
    reverse(a.begin(), a.end());
    vector<int> b;
    b.push_back(a[0]);
    for(int i = 1; i < a.)
    // dang do
}